\edef\refsec{\refmodelsec}
\edef\refeq{\refmodeleq}

\section{Informal Savings Model}\label{\refsec}
The following model is a variation of the informal analytical model. The only difference in the model structure is that the informal households $j=0$ now have access to an informal savings vehicle. Their savings are not added to the formal capital (thus do not enter aggregate $s_t$). However, they still earn an interest rate $R_t^0$ on their savings that is assumed proportional to the formal savings market for simplicity. It is straightforward to show that the economic equilibrium solution is identical \textit{except} for the informal households. 


\smalltitle{What changes relative to the analytical model.}
Because the change is confined to the informal households, several important features carry over to this version, including the general structure of the economic equilibrium and formal consumption functions. However, in the political problem, the informal savings ratio $s_{t-1,0}/s_{t-1}$ now enters the problem, which depends on \textit{current and future} policy choices; The implication is that the continuation policy function in politico-economic equilibrium adds an endogenous state variable $s_{t-1,0}/s_{t-1}$. The distribution $s_{t-1,i}/s_{t-1}$ could be avoided because it could be written \textit{entirely} as a function of future policies. Now, instead, we know that a choice of $\tau_t$ shapes the choice of $s_{t,0}/s_t$ directly, which enters the choice objective function for $t+1$ directly; this makes it an endogenous state variable for the political problem. 


\input{model_setup}
\input{model_competitiveequilibrium}
\input{model_PEE}
\input{model_finitehorizon}
\input{model_calibration}
