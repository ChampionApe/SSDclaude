# InformalSavings

Informal savings model variant (see project overview in root `CLAUDE.md`). Not started.

## Files
None yet. Expected to follow the same three-file pattern as `informalAnalytical/` (`model.py`, `base.py`, `policy.py`), documented in a matching `writing/InformalSavings_docs.tex`.

## Implementation status
Not started.
